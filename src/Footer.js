import { Box } from '@mui/material';
import ThemeToggle from './ThemeToggle';

export default function Footer () {
  return (
    <footer>
      <Box>
        Footer
      </Box>
      <ThemeToggle />
    </footer>
  )
}